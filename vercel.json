{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run prisma-generate && npm run build"
      }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "src/main.ts" }]
}
